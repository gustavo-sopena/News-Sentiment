[project]
name = "news-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.7",
    "pytest>=7.0",
    "requests>=2.28",
]
